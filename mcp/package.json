{
  "name": "usesend-mcp",
  "version": "1.0.0",
  "description": "MCP server for useSend — lets AI agents send transactional email and manage domains, contacts, campaigns, and analytics on an EverJust useSend/SES instance.",
  "type": "module",
  "bin": {
    "usesend-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
